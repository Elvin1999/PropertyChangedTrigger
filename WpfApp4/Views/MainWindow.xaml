<Window x:Class="WpfApp4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Button Width="200" Height="50" Content="Add" FontSize="20" Command="{Binding AddCommand}" Background="Aqua" Margin="20"></Button>
            <Button Width="200" Height="50" Content="Update" FontSize="20" Command="{Binding UpdateCommand}" Background="Aqua" Margin="20"></Button>
            <Button Width="200" Height="50" Content="Reject" FontSize="20" Command="{Binding RejectCommand}" Background="Aqua" Margin="20"></Button>
        </StackPanel>
        
    </Grid>
</Window>
